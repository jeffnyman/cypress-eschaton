describe("Simple Spec Pedagogy", () => {
  it("recognizes that truth is true", () => {
    expect(true).to.equal(true)
  })

  it("understands falsity cannot be true", () => {
    expect(false).to.not.equal(true)
  })
})
